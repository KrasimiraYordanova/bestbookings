<section>

    {{#with room}}
    <h1>{{name}} - Details</h1>
    <div><img src="{{imgUrl}}" alt=""></div>
    <p>{{description}}</p>
    <p>Price per night: {{price}}$</p>
    {{/with}}
   
</section>